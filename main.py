#Purpose: Entry point for running the program

from cli import handle_user_choice # Import the CLI controller

if __name__ == "__main__": # If this script is run directly
    handle_user_choice() # Run the menu when the script is executed.